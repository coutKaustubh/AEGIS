# Run outputs

AEGIS creates one `run_<id>/` directory per completed request. Each run may
contain the final result, metadata, trace, and references to verified
artifacts. These files are generated and can be removed when no longer needed.
