# Live AEGIS coding-agent fixture

This intentionally contains a failing slug normalization implementation. The
agent should inspect the module and tests, run pytest, diagnose the failure,
repair the implementation, rerun pytest, and verify the result.
