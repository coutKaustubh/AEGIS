# Registered artifacts

Generated documents and other reusable artifacts may be registered here with
provenance metadata. Artifacts must remain inside the workspace boundary and
pass format/content verification before being reported as successful.

`create_file` automatically registers files created under this directory in
`artifacts.json`, including their SHA-256, producer tool, size, and verification
status. `sample_report.json` is a small local example.
