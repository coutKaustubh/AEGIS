# Generated run outputs

AEGIS creates one directory per run here with `result.txt`, `metadata.json`,
`trace.json`, and optionally `network_report.json`. These files are local
runtime state and are intentionally ignored by Git.
